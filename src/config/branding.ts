/**
 * مسار الشعار الموحّد (مجلد `public`).
 * انظر `public/BRANDING.txt` — المكوّن `VoleraLogo` يعرض نصاً احتياطياً إن فشل التحميل.
 */
/** إصدار للتخزين المؤقت — غيّره عند استبدال ملف الشعار */
export const VOLERA_LOGO_PATH = '/volera-logo.png?v=20260407';
